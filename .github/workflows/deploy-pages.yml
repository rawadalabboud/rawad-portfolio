# Disabled: legacy gh-pages branch deploy is used instead (npm run deploy:gh).
# GitHub Actions Pages deploy was stuck queued/cancelled on this repo.
name: Deploy GitHub Pages

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Deploy with npm run deploy:gh"
